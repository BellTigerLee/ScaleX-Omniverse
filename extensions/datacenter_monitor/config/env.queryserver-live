# QueryServer live bridge profile.
# Use with: DC_PROFILE=queryserver-live <omniverse launch command>
# This profile targets the current QueryServer VM bridge Kafka contract, not kafka-dummy.

# Kafka bootstrap exposed for external clients. Keep this as host:port, no http:// scheme.
CLUSTER_HOST=10.32.161.108
KAFKA_NODEPORT=30892

# QueryServer topology API. Adjust the host if QueryServer runs somewhere else.
TOPOLOGY_URL=http://10.32.161.108:8000/topology

# Production/PoC must not fake-map unknown telemetry nodes onto arbitrary USD prims.
DEV_FAKE_NODE_MAPPING=false
